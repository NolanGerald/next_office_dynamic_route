export interface SiteDetails {
    title: string;
    headerText: string;
}

export const config: { siteDetails: SiteDetails } = {
    siteDetails: {
        title: "3D Office",
        headerText: "3D Office",
    }
};